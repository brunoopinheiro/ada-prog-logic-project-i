# Deve ser a funcao base que vai ler o input e retornar a tabela do campeonato
def championship_parser(input_json: dict):
    """
    Função principal, que recebe o dict de input do campeonato e monta a tabela final com a classificação dos times, ordenada seguindo os critérios de classificação do campeonato.
    """
    output_table = []
    rodadas = input_json.keys()
    return rodadas
